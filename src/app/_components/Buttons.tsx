import React from "react";

type Props = {};

export default function Buttons({}: Props) {
  return (
    <div>
      <button className="rounded bg-blue-500 px-4 py-2 font-bold text-white outline-red-500">
        Button
      </button>
    </div>
  );
}
